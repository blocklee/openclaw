# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- Assistant: **TLSNotary** | ❌ 不需要 | ⚠️ 部分 | ❌ 仅数据 | ✅ 中等 | 链下服务信任 | --- ## ⚠️ 暗坑：TEE 的"硬件信任"并非万能 主公若在考虑从 TEE 转向替代方案，有一事不得不察： **TEE 最脆弱的地方恰恰就是它的卖点** — 对硬件厂商的信任。Intel SGX 已被多次攻破（Plundervolt、Smash、AEPIC Leak），而且 Intel 停用了 SGX 的远程证明服务。AMD SEV 也有 side-channel 漏洞。硬件 TEE 的安全边界并不像宣传的那么绝对。 [confidence=0.49 evidence=memory/.dreams/session-corpus/2026-05-13.txt:4-4]
- Assistant: 3：冷启动延迟 TDX VM 启动 + Attestation → 约 5-15 秒。 用户等一个 Skill 执行需要 5 秒冷启动，体验不行。 **解决方案：预启动 TDX Pool** ``` 始终维持 N 个已 Attest 的 TDX VM ↓ 用户请求 → 分配一个空闲 VM → 注入 Skill → 执行 ↓ 冷启动 < 500ms（VM 已在池中） ``` --- ## 四、给创作者的实际开发流程设计 假设方案确定为 **WASM in TDX**： ``` 1. echo dev init # 初始化 Skill [confidence=0.49 evidence=memory/.dreams/session-corpus/2026-05-14.txt:13-13]
- Assistant: A：WASM Sandbox in TDX（推荐）** ``` TDX VM └─ ECHO Runtime (Rust/Go) └─ WASM Runtime (Wasmtime / WasmEdge) └─ Skill A (WASM) → 细粒度隔离 └─ Skill B (WASM) → 细粒度隔离 ``` - 优点：每个 Skill 天然隔离，WASM Gas 可计量 - 缺点：WASM 生态限制（部分 Python 库不支持） **方案 B：gVisor / Firecracker in TDX** ``` TDX VM [confidence=0.49 evidence=memory/.dreams/session-corpus/2026-05-14.txt:12-12]
