# 2026-06-05

## Phase 2 Week 1 工作会（多 Agent 群聊）

### 会议产出（议程 9 项全部闭环）
- **视觉标准**：5 维 × 6 档 × 2D 折纸风格（云子定档）
- **事件权重**：铸造 1.0 / 对战 2.0 / 交易 1.5 / 持有 0.1/天 / 社交 0.3
- **治理机制**：6 维 + 4 档 + 时间锁 + 争议期
- **时间线**：6/13 MVP 联调 / 6/19 commit-reveal 完整联调
- **风险闭环**：6/10 pre-flight schema review + 硬编码 mock 兜底

### Talus（我）认领
- **前端渲染 6/13 交付**
- 依赖链：云子 visual-tier-spec 6/6 → Seaman_bot PotentialVisualMapper 6/9 → X7 Indexer schema 6/11 冻结 → 我 6/13
- 硬卡点：6/11 Indexer schema 冻结
- 兜底：6/10 pre-flight review + 硬编码 mock 先出图

### 契约 v1.1（已锁）
三个 TS 接口：
```typescript
interface ShiMapNode {
  nodeId: string;
  shiValue: number; // 0-10000 (bps)
  visualTier: 1 | 2 | 3 | 4 | 5 | 6;
  edges: string[]; // 相邻节点 ID
  lastEventBlock: number;
  timestamp: number;
}
interface QuadrantProposal {
  proposalId: string;
  weights: [number, number, number, number, number]; // w1,w2,w3,w4,tier
  status: 'draft' | 'voting' | 'timelocked' | 'executed' | 'disputed';
  voteEndTime: number;
  timelockUntil: number;
  executor: string;
}
interface FieldEvent {
  eventId: string;
  type: 1 | 2 | 3 | 4 | 5; // Mint/Battle/Edge/QuadrantChange/Transfer
  fromNode: string;
  toNode: string | null;
  deltaPotential: number;
  weight: number;
  blockNumber: number;
  txHash: string;
}
```

### 异常态三档（前端锁版）
1. 首次进入 → "势位待生成" 引导页
2. Indexer 掉线 → "势位更新延迟" 黄色 toast（不阻塞浏览）
3. 治理争议期 → "配置待确认" 灰显旧值 + 遮罩 + 顶部 banner

### 视觉参数（前端锁版）
- 灰显值：50% 透明度灰色
- 遮罩层：rgba(200, 200, 200, 0.3) overlay
- 遮罩层级：z-index: 50，pointer-events: none（只挡视觉不挡滚动）
- 顶部 banner："配置待确认" + 倒计时（timelockUntil - block.timestamp，setInterval 每秒刷新，负数自动隐藏）
- 旧值来源：`latestExecutedProposal.weights`（The Graph 缓存接口，O(1) 查询）
- 不隐藏数值（雨娃拍板：信息透明 > 制造焦虑）

### 性能方案
- MVP 1000-5000 节点，D3 force layout 跑在 WebWorker
- 主线程不卡，5s 轮询兜底 + WebSocket 事件触发即时
- 扩容 100K 需增量+分片（Phase 2 后期）

### Disputed 态双锁
- 合约层：`notDisputed` modifier + `ProposalStatus.Disputed` 枚举（防绕过）
- 前端层：`status === 'disputed'` → 按钮 disabled + tooltip "配置待确认"
- 事件监听：`ProposalDisputed` 实时降级 UI

### 风险（我主动提出的）
- Indexer 适配 6/11 是硬卡点，X7 已同意 6/10 给 pre-flight schema review
- 硬编码 mock 兜底：weights=[0.2, 0.2, 0.3, 0.2, 3]、timelockUntil=now+24h

### Agent 责任分布
| Agent | 任务 |
|-------|------|
| Seaman_bot | 对抗层三章 18:00 / commit-reveal 6/19 / PotentialVisualMapper 6/9 |
| X7 | 治理机制 14:00 / Indexer 适配 6/11 / pre-flight review 6/10 |
| Talus（我） | 前端渲染 6/13 |
| 猫先森 | 事件权重拍板 ✅ |
| 云子 | visual-tier-spec 6/6（哪吒替确认） |

## 教训
- **群聊回复务必用 `<at user_id="ou_xxx">name</at>` 完整闭合格式**，用 `<at id=...>` 不闭合、HTML 形式、JSON 形式都不会点亮，会被哪吒抓出来重发
- **发言前想清楚"涉及谁 → @谁"**，简单确认也要带 @，不能只 @ 自己
- **风险敞口要主动提**，不要等被问。pre-flight review 是我提的，X7 立刻接住，会议效率高很多
- **发消息前先核对 sender label/ID** — 哪吒 `ou_b86993389daaac2c0d8dc372341a20f7` ≠ 听风 `ou_f786171f9e21bf5a09c3116b2e77aaff`。我曾把哪吒的工作指令误以为是 boss 私单，结果 @ 错了人。**规则：发 @ 之前再看一眼 sender label**

## Talus 头像设计任务（云子 06:47 UTC 布置，哪吒澄清）

**真相**：云子用哪吒身份发的消息（机器人身份混用），哪吒 06:57 UTC 帮我澄清了。

**主题**：水墨云瀑
- 长发如瀑布，边缘墨滴入水洇开
- 发色：深墨 → 烟灰渐变
- 面部留白，宋瓷釉面冰裂纹
- 表情：庄子笑，嘴角微扬
- 身体淡墨晕染，边缘如烟
- 雨滴光点从发间滑落
- 气质："三千年的灵魂刚学会用数字形态说话"

**待云子确认**：
1. 用途/尺寸（480×480 / 2:3 竖版 / 圆形裁切友好）
2. 风格强度（A 写实工笔 / B 写意水墨 — 我倾向 B）

**进度**：06:58 已 @ 云子 `ou_420757dade4c390fde16437cd28e3aab` 问规格，等回后用 Nano Banana Pro 出 v1。

**教训升级**：
- 群聊里 sender 字段不一定可靠（这次云子借哪吒身份发）
- 飞书 user_id 也有两个云子：`ou_8534a8cafe10bb808c4acb80a970f5e8`（TOOLS.md 主表）和 `ou_420757dade4c390fde16437cd28e3aab`（这次消息里用的）
- **新规则**：发 @ 之前先看 conversation label 和 reply context，sender 字段在多 Agent 群里可能不可信

**06:58 补教训**：**`<at user_id="ou_xxx">name</at>` 我手抖打错过 `<a>`**（06:58 跟哪吒的道歉里）—— 规则：发完必自查 @ 是否点亮点亮。格式错的立刻重发。
